#include "array_string.h"
#include "core/string/string.h"





void  ArrayString::add(const String& p_value) {
     p_strings.push_back(p_value);
}

bool ArrayString::contains(const String& p_value) const {
    return;
}
